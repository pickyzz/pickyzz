name: Latest blog post workflow
on:
  schedule:
    - cron: 0 17 * * *
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: JasonEtco/rss-to-readme@v1
        with:
          feed-url: https://pickyzz.dev/feed
          readme-section: feed
